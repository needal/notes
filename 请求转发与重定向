请求转发和重定向后面的代码依旧会执行,而请求转发是先执行页面跳转，再执行后面的代码；重定向是先执行后面的代码，再执行页面跳转。

重定向是两次请求,不会共享同一个request和response对象,会显示重定向以后url，可以进行跨域请求。
请求转发是一次请求，共享同一个request和response对象，不会改变url，不能进行跨域请求。
note:对于sendRedirect如果传递的URL以"/"开头，他是相对于整个Web站点的根目录；对于forward如果传递的URL以"/"开头，它是相对于当前Web应用程序的根目录。
